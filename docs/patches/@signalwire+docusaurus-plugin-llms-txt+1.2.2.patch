diff --git a/node_modules/@signalwire/docusaurus-plugin-llms-txt/lib/index.js b/node_modules/@signalwire/docusaurus-plugin-llms-txt/lib/index.js
index 5888e45..148f8a5 100644
--- a/node_modules/@signalwire/docusaurus-plugin-llms-txt/lib/index.js
+++ b/node_modules/@signalwire/docusaurus-plugin-llms-txt/lib/index.js
@@ -1,3 +1,5 @@
+import fs from 'fs';
+import path from 'path';
 import { flattenRoutes } from '@docusaurus/utils';
 import { registerLlmsTxt, registerLlmsTxtClean } from './cli/command';
 import { getConfig, validateUserInputs } from './config';
@@ -115,6 +117,17 @@ export default function llmsTxtPlugin(context, options = {}) {
     }
     return {
         name,
+        async contentLoaded({ actions, content: _content }) {
+            actions.setGlobalData({
+                copyContentDataUrl: '/copy-content-data.json',
+                copyContentConfig: undefined,
+                siteConfig: {
+                    url: context.siteConfig.url,
+                    baseUrl: context.siteConfig.baseUrl,
+                    trailingSlash: context.siteConfig.trailingSlash ?? false,
+                },
+            });
+        },
         async postBuild({ outDir, siteDir, generatedFilesDir, siteConfig, routes, }) {
             const config = getConfig(options);
             const log = createPluginLogger(config);
@@ -149,6 +162,19 @@ export default function llmsTxtPlugin(context, options = {}) {
                     relativePaths: config.content?.relativePaths !== false,
                 }, enhancedCachedRoutes);
                 log.success(`Plugin completed successfully - processed ${result.processedCount} documents`);
+                // Write route map for the copy-page-button theme component
+                const mdFiles = flattenRoutes(routes)
+                    .map(r => r.path)
+                    .filter(p => {
+                        const normalized = (p === '' || p === '/') ? '/index' : p.replace(/\/$/, '');
+                        return fs.existsSync(path.join(outDir, normalized + '.md'));
+                    })
+                    .reduce((acc, p) => {
+                        const normalized = (p === '' || p === '/') ? '/' : p;
+                        acc[normalized] = true;
+                        return acc;
+                    }, {});
+                fs.writeFileSync(path.join(outDir, 'copy-content-data.json'), JSON.stringify(mdFiles));
             }
             catch (error) {
                 const errorMessage = getErrorMessage(error);
